class Ray {
    /*
     * Create a ray starting from origin with given unit direction.
     *
     * origin: The ray's origin
     * direction: The ray's direction
     */
    constructor(origin, direction) {
        this.origin = origin;
        this.direction = direction;
    }
}